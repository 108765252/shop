const MOCKURL = 'http://www.weichuang.com';

// eslint-disable-next-line no-unused-vars
const URL = { //URL对象的方法是getVarietyItem
    getVarietyItem: MOCKURL + 'getVarietyItem',
    getHotProducet: MOCKURL + 'getHotProducet'
};

export default URL;